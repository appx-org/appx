/** Barrel export for agent interaction components. Reusable across web, mobile, desktop. */
export { default as SessionList } from './SessionList';
export { default as ChatPanel } from './ChatPanel';
